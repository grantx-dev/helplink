{
  "name": "helplink",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "internal/*"
  ],
  "dependencies": {
    "@helplink/db": "workspace:^"
  },
  "devDependencies": {
    "@types/node": "^22.7.4",
    "turbo": "^1.4.0"
  },
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint"
  }
}

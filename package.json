{
  "name": "helplink",
  "version": "1.0.0",
  "private": true,
  "workspaces": ["apps/*"],
  "devDependencies": {
    "turbo": "^1.4.0"
  },
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint"
  }
}
